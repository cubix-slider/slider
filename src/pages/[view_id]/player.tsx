import React from 'react';

import { SlidePlayerViewerPageContainer } from '../../containers/SlidePlayerViewerPageContainer';

const SlidePlayerViewer = () => (
  <SlidePlayerViewerPageContainer />
);

export default SlidePlayerViewer;
